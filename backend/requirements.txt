# --- Core API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
python-multipart==0.0.20

# --- Database ---
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# --- Auth / security ---
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4

# --- HTTP / math ---
httpx==0.28.1
numpy==2.2.1

# --- Config ---
python-dotenv==1.0.1

# --- Tests / lint ---
pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.4

# Local semantic embeddings (sentence-transformers + torch, ~2GB) live in
# requirements-embeddings.txt so small hosts can run with EMBEDDING_PROVIDER=hashed.
# Optional feature deps:
# presidio-analyzer==2.2.355 ; presidio-anonymizer==2.2.355 ; pypdf==5.1.0
